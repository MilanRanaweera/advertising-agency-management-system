package com.sliit.aams.config;

import org.springframework.context.annotation.Configuration;

/**
 * Shared Spring MVC / CORS / static resource configuration goes here.
 * TODO: configure CORS if the frontend is served separately.
 */
@Configuration
public class WebConfig {
}
